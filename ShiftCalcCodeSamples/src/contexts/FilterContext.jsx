// src/contexts/FilterContext.jsx
// This is now just a re-export file to maintain backward compatibility

// Re-export the refactored FilterContext from the new modular structure
export { FilterProvider, useFilter } from "./filter";